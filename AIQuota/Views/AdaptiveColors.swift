import SwiftUI

extension Color {
    static let brand = Color("BrandAccent")
    static let gaugeAccent = Color(nsColor: .systemPurple)
    static let warningAmber = Color(nsColor: .systemOrange)
    static let critical = Color(nsColor: .systemRed)
}
