## What's New in 1.9.26

- **Reliable Claude reauthentication:** when Claude's embedded web session has expired, AIQuota now clears it before opening Sign In so you get a fresh login screen instead of an apparently signed-in Claude window that cannot refresh usage.
