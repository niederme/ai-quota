## What's New in 1.9.21

- **Smoother updates** — AIQuota keeps Codex and Claude Code connected across app updates when your existing sessions are still valid, and adds safeguards to prevent broken updates.
- **Cleaner setup** — first-run setup is shorter and puts optional choices where you expect them.
